- name: install mysql client
  ansible.builtin.yum:
      name: mysql
      state: installed


- name: Load mysql Schema
  ansible.builtin.shell: mysql --host mysql.joindevops.shop -uroot -pDevops@1 < /app/schema/{{component}}.sql 
